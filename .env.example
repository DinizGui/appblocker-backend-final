# Server
PORT=4000
HOST=0.0.0.0

# Database (SQLite)
DATABASE_URL="file:./dev.db"

# JWT (use a long random string in production)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Google Sign-In (Web Client ID - same as used in the app for idToken verification)
# Get it at: https://console.cloud.google.com/apis/credentials
# Create "OAuth 2.0 Client ID" type "Web application" to get client ID for backend verification.
# The Android app will use a separate Android OAuth client; both can use the same Web client ID for token verification.
GOOGLE_CLIENT_ID=

# CORS (optional, default *)
# CORS_ORIGIN=http://localhost:8081
